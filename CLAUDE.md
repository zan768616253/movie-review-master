# Claude Code Instructions

Use [docs/agent-rules/python-environment.md](docs/agent-rules/python-environment.md) as the source of truth for Python command execution and dependency management in this repository.

If a task involves Python, pip, pytest, uvicorn, or any other installed entry point, follow that rule before doing anything else.
